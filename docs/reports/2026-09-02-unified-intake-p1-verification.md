# Unified Intake x P1 Phase 1 Verification

Baseline: `687bbdd0` (worktree state before this verification fix)
Merge target: `main`

## Commands and results

- `cd itsm-backend && go test ./... -count=1`: PASS; all listed packages returned `ok` or `[no test files]`.
- `cd itsm-backend && go build ./...`: PASS; exit 0 with no output.
- `cd itsm-backend && go test ./migration -run 'TestUnifiedIntakeMigrationsRegisteredInOrder|TestMigration024ServiceCatalogTargetClassAuthorityAssets|TestMigration024ServiceCatalogTargetClassAuthorityMatchesRegisteredSQL' -count=1`: PASS.
- `cd itsm-frontend && npm run type-check`: PASS; `tsc --noEmit` exit 0.
- `cd itsm-frontend && npx jest --silent --runInBand`: PASS; all suites completed successfully.
- `cd itsm-frontend && npx jest src/lib/api/__tests__/service-catalog-api.test.ts --runInBand`: PASS; service-catalog API contract suite passed, including missing `targetClass` rejection.
- `cd itsm-backend && go test -tags integration_postgres -v ./handlers/intake -run E2E -count=1`: PASS with no tests to run; no E2E test matched this selector.
- `cd itsm-backend && go test -tags integration_rls -v ./database/rls/... -count=1`: PASS; non-database tests passed, PostgreSQL cases skipped because `RLS_TEST_DSN` was not set.
- `cd itsm-backend && go test -tags integration ./migration -run TestServiceCatalogTargetClassMigrationRejectsUnknownITSMType -count=1 -v`: BLOCKED by environment; the test requires `ITSM_TEST_DB`, which was not set.
- `find . -type f -name '*.go' ... grep -nE 'GenerateIncidentNumberForIntake|WorkItemNumberFunc|highestLevel\\(|Status: "open"|ComputeTargetClass|generateIncidentNumber\\(|validateIncidentAssignee\\(|srIncidentBridge|\\.CreateIncident\\('`: no production matches for retired Intake allocators, `highestLevel`, `ComputeTargetClass`, `srIncidentBridge`, or private incident-generation helpers. Remaining matches are existing tests, comments, the Intake creator's intentional initial status, and direct `IncidentService.CreateIncident` test coverage.
- `git diff --stat main -- itsm-backend/ent/schema`: six changed schema files: the three Intake schemas, `servicecatalog`, and two pre-existing schema files in the reconciled branch. No unreviewed service-catalog extension schema was found.

## Acceptance criteria (spec section 5.6)

1. [x] `workitemnumber.Allocator` is the sole source for `tickets.ticket_number`; `incidents.incident_number` remains a separate identifier generated by the incident service.
2. [ ] Migrations `023`, `024`, and `025` are ordered correctly; `024` backfills valid legacy values, rejects unsupported or NULL `itsm_type`, enforces `target_class` `NOT NULL` plus CHECK, and drops `itsm_type`. Static registry/asset checks passed, but real PostgreSQL execution remains environment-blocked in this run.
3. [x] Incident priority/status parity and DTO field handling are covered by the Intake and incident tests.
4. [x] Service Request custom fields use the `entity_type=ticket` path.
5. [x] `Idempotency-Key` is mandatory across the converged entry points, with controller, BPMN, catalog, and frontend coverage in the existing test suites.
6. [x] Enumerated HTTP, BPMN, and catalog-derived Incident/Change creation entry points converge on Intake.
7. [x] Change creation produces a real `ent.Change` extension.
8. [x] The backend regression suite is green.
9. [x] Backend build and frontend type-check/Jest are green.
10. [x] The Ent schema diff was reviewed; the service-catalog retirement and Intake schemas are addressed, with no missed Task 14 schema conflict identified.

## Remaining verification prerequisites

- Re-run the Task 14 PostgreSQL migration tests with `ITSM_TEST_DB` configured, including the unsupported historical-value case.
- Re-run the RLS integration cases with `RLS_TEST_DSN` configured.
- Add or invoke the repository's intended `handlers/intake` PostgreSQL E2E selector when its tagged E2E tests are available; the requested `-run E2E` selector currently matches zero tests.