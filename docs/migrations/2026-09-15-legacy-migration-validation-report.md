# 旧 ITSM 主数据迁移验证报告（克隆库 / 运行环境）

- **日期**：2026-09-15
- **验证目标**：运行环境所用库 `itsm_ga_ready`（容器 `ga-itsm-20260914`，角色 `ga_runtime`）
- **真值来源**：SOP 指定的旧系统导出文件
  - `itsm_departments.json` sha256 `f8b3fbdaa1217672…`（5,272 条，文件时间 2026-08-04）
  - `itsm_users.json` sha256 `fe794d35abcbffb9…`（14,393 条，文件时间 2026-08-19）
- **验收口径**：逐项差异归因——每个差异桶要么有可信解释与证据，要么列入"待裁定"
- **证据**：`docs/migrations/2026-09-15-legacy-migration-validation-evidence.json`（由
  `scripts/verify_itsm_migration_data.py` 生成，全程只读）
- **说明**：本次**未修改任何数据**；修复迁移属于另一个任务

## 1. 血统：只有一批迁移，没有重复执行

| 库 | 部门 | 部门创建时间 | 用户 | 活跃 | 工号式用户名 |
| --- | --- | --- | --- | --- | --- |
| `itsm_ga_ready`（运行环境） | 7,975 | 2026-08-19 | 7,862 | 7,834 | 5,537 |
| `itsm_migration_20260914` | 7,975 | 2026-08-19 | 7,834 | 7,834 | 5,537 |
| `itsm`（DEV） | 7,975 | 2026-08-19 | 7,834 | 7,834 | 5,537 |
| `itsm_baseline_20260908` | 7,975 | 2026-08-19 | 7,834 | 7,834 | 5,537 |

四个库完全一致，且所有迁移行都落在 **2026-08-19 单一批次**：迁移只执行过一次，克隆是忠实的。
运行环境比其它库多 28 个用户（2026-09-14 创建，为环境准备/测试账号），不是迁移数据。

**影响**：上表中"多出 2,703 个部门"不是克隆造成的，而是 2026-08-19 那次迁移本身的输入特征。

## 2. 差异归因

### 2.1 部门（导出 5,272 / 库内 7,975）

| 桶 | 数量 | 归因 |
| --- | --- | --- |
| 精确匹配（`departmentId` = `departments.code`） | 4,975 | — |
| 仅导出有（库中无该 code） | 297 | 其中 **236 个的名称在库中存在** → 同一组织在库内是**另一个 id**；185 个为 32 位 UUID 形态、112 个为编码形态 |
| 仅库中有 | 3,000 | **已基本解释**：1,083 个是库内 code 的中间层级；**2,323 个（77.4%）的名称出现在用户 `departmentName` 路径分段中**（导出部门文件是 5,272 节点的树，用户的路径携带了导出之外的组织节点，迁移时一并建为部门）；677 个仍待解释 |
| 库内重复 code | 0 | ✅ |

**归因结论**：与手上这份 **2026-08-04** 部门导出相比，库内部门集合有约 3,000 条多出、297 条缺失，
但名称高度重合（87%）。这与"2026-08-19 那次迁移使用的部门快照比这份文件更新/不同"一致。
**无法证实**：迁移当时的输入文件不在机器上，且迁移 CLI（SOP 引用的 `cmd/migrate_legacy_itsm`）
**从未提交到仓库**，无法复现其映射规则——列为待裁定项。

### 2.2 用户（导出 14,393 行 / 唯一用户名 14,388 / 库内 7,862）

| 桶 | 数量 | 归因 |
| --- | --- | --- |
| 精确匹配（`userName` = `users.username`） | 7,816 | — |
| 仅导出有且为活跃（`status=userstatus01`） | **1,243**（补迁前为 1,261） | 判别特征：**1,243 个无 `HR_USERID`**（迁移口径＝只迁 HR 关联员工，非缺陷）；原 **18 个有 `HR_USERID` 的真遗漏已于 2026-09-15 补迁完成**（见 §4） |
| 仅导出有且为非活跃 | 5,311 | 与"只迁移活跃用户"口径一致 ✅ |
| 仅库中有 | 46 | 35 个在批次之后创建（2026-08-19 之后）、8 个非旧系统命名、**2 个旧系统形态但导出中不存在**（`sha256:d934efe3`、`sha256:24b09808`→待裁定）、1 个属 tenant 2 |
| 库内重复用户名 | 0 | ✅ |

导出侧活跃口径为 **9,011**（唯一用户名；行数 9,014），其中 **1,261（14%）未落库**。

### 2.3 邮箱域名改写：**已确认有意为之，不是缺陷**

| | 域名分布 |
| --- | --- |
| 导出文件 | `kerryeas.com` 7,541、`keas.kln.com` 4,265、`kln.com` 1,022、`tci.com.cn` 210、`kerrylogistics.com` 177，另有 **168 条域名为字面量 `无`** |
| 库内 | **`keas.kln.comm` 7,826**（其余 36 条为环境/测试账号域名） |

- 8,816 个匹配用户的邮箱**精确一致数为 0**，库内一律为 `<局部名>@keas.kln.comm`。
- 该改写**同时存在于 DEV `itsm`、专用迁移库和克隆库** → 属**迁移时行为**，不是克隆或环境准备造成的匿名化。
- 影响：真实邮箱丢失，"按邮箱通知/按邮箱识别用户"在库内均指向同一个虚构域名。
- **用户裁定（2026-09-15）**：**有意为之**——防止开发环境向真实地址发送邮件。故本项**不计缺陷**，
  库内该域名保持现状。

### 2.4 其它字段与结构

| 检查 | 结果 |
| --- | --- |
| 姓名（`realName` → `name`） | 7,815 / 7,816 一致 ✅ |
| `active` 标志 vs 导出 `status` | 7,750 / 7,816 一致，**66 条不一致**（待裁定） |
| 部门绑定 | **正确的绑定键是导出 `departmentUnit`**（不是叶子 `departmentId`）：与库内部门 code 一致 **7,470** 条、不同 30 条、未绑定 316 条。早前按 `departmentId` 比对得 0 命中系**比错键**，已更正 |
| 密码 | **7,861 bcrypt**、**0 旧密码哈希复用** ✅；1 条非 bcrypt 为测试账号 `kaf_closeout_t2_20260831` |
| 库内重复邮箱 | **17** 条（唯一性约束缺口） |
| 部门层级 | 7,975 个父节点全部可解析、**0 环**、25 个根 ✅ |
| 归属 | tenant 1 = 7,861 用户；tenant 2 = 1 用户（环境准备账号） |

### 2.5 仓库口径未解释项

SOP 写"仓库 246 个"，库内 `org_type=warehouse` 为 **737**；导出文件 `departmentType` 只有 1（5,270）
与 0（2），没有 type=2。**已自行推出实际规则：按名称判定**——737 个仓库中 **728 个名称含"库/仓/物流"**，
而 3,000+ 非仓库行中 **0 个**命中该模式；导出文件中名称符合条件的节点为 549 个（与库内数量差来自 §2.1
的快照漂移）。9 个未见名称特征的仓库行待复核；SOP 的 246 与该规则不符，属过期数字。

## 3. 裁定结果（2026-09-15）

| # | 项 | 裁定 |
| --- | --- | --- |
| 1 | 1,261 个活跃旧用户未迁移 | 1,243 个属**口径**（无 `HR_USERID`）；**18 个属遗漏，已于 2026-09-15 补迁完成** |
| 2 | 邮箱域名统一改写 | **有意为之**（防止开发环境发信）→ 不计缺陷 |
| 3 | 部门快照漂移 | 08-19 的输入文件不在机器上、迁移 CLI 未入库 → **待补输入/文档**，无法复现 |
| 4 | 库内 2 个旧系统形态用户名在导出中不存在 | 待复核 |
| 5 | 66 条 `active` 差异、17 条重复邮箱、316 个无部门归属 | 用户裁定 **"记录即可，不需要处理"**（2026-09-15）→ 本报告即为记录，**不修改数据** |
| 6 | 737 个 warehouse 与 SOP 的 246 不符 | 规则已推出为**按名称**；SOP 数字过期；9 行待复核 |

### 附：18 个待补迁用户（均有 `HR_USERID`，邮箱在库中不存在）

`D78089 D83223 D83360 D83364 D83473 H83436 H83510 D84725 D84735 H82255 H84813 H84410
H84946 H84947 D33743 D44967 D50002 D52745`

（其部门在库中均存在；导出内邮箱无重复，因此不是去重或改名造成。）

## 4. 复现方式

```bash
python3 scripts/verify_itsm_migration_data.py --self-test          # 不连库的自检
python3 scripts/verify_itsm_migration_data.py \
  --evidence-out docs/migrations/2026-09-15-legacy-migration-validation-evidence.json
```

工具全程只读（`BEGIN READ ONLY`，不建临时表），数据库口令从已准入的 profile 读取且不打印；
DEV 库对比使用 SOP 中已记录的开发口令。

## 4. 18 个遗漏用户的补迁（2026-09-15 已完成）

- **方式**：走产品自身接口 `POST /api/v1/users`（经 3010），非直写库 → 口令加密、字段校验、审计均由产品完成
- **映射来源**：从现有 7,816 个已迁移用户**反推**（非猜测）：
  `role=end_user`（7,812/7,816）、绑定键=`departmentUnit`；原批次 `gender`/`is_leader`/`function_line`/
  `manager_id`/`phone` 均为空或不可解析（`leaderId` 是 HR 标识），故**不发送**这些字段
- **邮箱**：沿用你确认过的开发环境改写规则（`<局部名>@keas.kln.comm`）
- **口令**：SOP 迁移默认口令（与原批次一致，未写入证据文件）
- **结果**：**18/18 创建成功**（id 7921–7938，HTTP 200 code 0）；DB 复核 18/18 存在、
  `active=true`、bcrypt 口令、tenant 1；部门绑定与导出 `departmentUnit` **18/18 一致**
- **可用性**：抽样登录 `D78089` / `D84725` / `H84410` 经 3010 全部 **200**
- **校验回归**：`only_export_active` **1,261 → 1,243**，`matched` 7,816 → 7,834，`db_rows` 7,862 → 7,880
- **回滚**：新建 id 已记录，可用 `PUT /api/v1/users/:id/status` 停用，或按证据文件中的 SQL 删除
- **工具与证据**：`scripts/backfill_legacy_users.py`（默认 dry-run、幂等、预检阻塞即拒写）；
  证据见本次提交的 backfill 记录

## 5. 输入与规则归档

导出文件身份、CLI 缺席证据与重建出的迁移规则见
[`2026-09-15-legacy-migration-input-and-rule-archive.md`](2026-09-15-legacy-migration-input-and-rule-archive.md)
与同名 manifest（仓库只存哈希与条数，个人数据不入库；原始文件已复制到受控私有归档目录）。
