# 首期配置功能收口状态

本工作树基于已对账运行时 `0788a9bb196ab37a8389b3f366bed9877b2f72c3`，用于集成已审修复；不是 G-A 或 G-B PASS 制品。不得因此切换应用连接。

## 用户已确认范围

- 单租户功能可用、完成上线收口；不开展 MSP 多租户产品扩展，现有身份与关联校验保留。
- 以新 ITSM 规范为准；无法确定的旧优先级配置保留差异。
- 使用现有分派与规范流程上线；旧路由后续处理，不迁近似旧路由。
- 历史工单、评论、附件、审批与流程实例不迁移；不执行 R038，不清理原库。

## 已完成的代码与工具证据

- ITSM 迁移工具 R1–R4：`7c8cee6fae181400573308bfb7e73043d11ed0b9`，独立复审 ADDRESS；真实独立 PG 语义测试 15/15、Node 30/30。
- KAF R5–R8：`e6fd8a50368a15505c98c8826dee5af975cef5c2`，独立复审 ADDRESS，相关离线测试 68/68。
- 根任务将原 pre-B0 备份恢复至专用 `gb_replay_review`，使用固定0788源重放五批：回滚预演、提交、即时同制品复跑均通过。原身份关键表与账本摘要不变；五条原子收据；没有给 G-A 现有数据补造历史收据。
- SLA 日历代码 `e290fd97c7f60838b966e0089ee843f01cb5afa8`，已独立复审并集成。真实函数时区/补班/有效期测试、异时区 race 测试通过；不含既有 BPMN 独立展示计时。
- 新日历配置位于工具分支 `e12b4151`，将既有19补班日接入，89假日保持原值，覆盖2024–2026。实际JSON经新parser复审，2027明确拒绝，届时必须续订；尚未应用G-A。

## 未完成项

1. 产品默认 `ticket_types` 定向初始化代码已完成并独立复审：源提交 `8ba80e186322d878d6801ff95b320b718f5fc356`，本分支集成 `99909766`。真实隔离 PG 6 顶层测试与 9 子例、CLI 2 例通过；真实 Prepare 消费验证通过。只补产品预置配置，真实有效管理员授权，原子审计，不运行整套 seed，不修改身份/初始化账本。**2026-09-15 09:36 CST 已在 G-A 定向初始化 12 类型，即时复跑新增 0**；数据库计数和 Prepare 测试不等于新建E2E。
2. SLA 实际启用：已确认 G-A 七个流程 binding 的 `sla_policy_id` 全空，当前创建路径不生成 deadline。已向用户询问采用现有 SLA 覆盖范围（Incident四档、Change两档、服务请求标准；其它类别暂不计时），回答前不应用依赖配置。
3. 运行时重新准入：G-A 的 `ga_runtime` 当前只有四个核心表的 DML；只读抽查配置、类型、流程、评论、附件及审计表均无该角色权限。这是 G-A 原结构准入范围，不能通过改用 owner 启动来绕过。需要明确最小运行权限并验证新的 EnvironmentRevision。
4. 实际新建、分派、规范流程、SLA、重启恢复验收；G-B 尚未通过，任务三物理PG合并未启动。

## 环境边界

旧候选应用和存储容器仍为停止状态，本次未启动。G-A目标已于2026-09-15定向初始化12个产品类型；SLA修订配置仍未应用。隔离测试实例 `gb-remediation-test-pg-20260914` 的三个测试库已登记到开发文档，均不能作为业务目标。

目前不合 main，不清理源数据，不修改旧交付 worktree；所有新运行时集成与验证均在独立目录完成。


## 首批里程碑（2026-09-14，未超过60分钟）

迁移审查 R1–R8 已关闭；SLA 日历和产品类型初始化代码完成且独立复审通过，并合入本独立分支。下一阶段仍依赖用户确认 SLA 覆盖范围，不推定未回复为批准。新 EnvironmentRevision、目标受控写入、应用启动与真实验收尚未执行。

绑定选择的独立证据位于 `/home/administrator/.local/state/itsm-task2-remediation-20260914/sla-binding-evidence/`：固定0788源码，四档优先级精确匹配、Change normal/emergency、默认绑定不截获均通过；显式流程key路径的SLA为空也已复现。G-A的八个默认目录流程key均为空，不触发此旁路；后续显式配置流程key需另行处理。

合并后验证：go build ./...、go test ./service -run ^TestSLACalendar -count=1、go test ./cmd/initialize_ticket_types -count=1 均通过。未运行或宣称全后端测试通过；旧基线全量测试差额见工具修复证据。


## 2026-09-15 用户最终范围决定

- 当前DEV使用 `itsm_config_baseline_20260908`；新目标 `itsm_ga_ready` 已存在，二者不得混称同一DEV克隆。
- 用户明确：26条现有DEV WorkItem及关联评论、附件、关系无需迁移；旧流程不迁移、不执行。源数据保留，不清空任何数据库或历史任务。
- 目标继续承接新规范下所需配置和基础数据；E2E新建记录，不为历史测试记录补齐目录、SLA周期或执行上下文。
- 通过可切换配置复用现有Backend与前端，保留原DEV配置供切回；不另建整套环境。同一Backend切换期间服务目标库，不宣称两个库同时由同一进程服务。
- 下一步收口连接配置、必要产品配置及运行权限，核对缓存/后台任务指向，再执行新建E2E。此决定不等于既有SLA功能差额已验收，也不授权源数据清理。


## 2026-09-15 Backend 切换执行批次（进行中）

单一目标：复用现有8080 Backend与3001前端，通过可切换配置连接已有itsm_ga_ready，并验证新建E2E；当前DEV数据库、配置和前端成果可恢复。**共享环境变更**仅由根任务执行：目标库必要配置初始化及经审权限增量、现有Backend切换；不修改源库，不清理旧记录/队列，不执行R038、不合main。

- 首先核对运行制品与源码；已发现当前7ed后端和目标94b30926存在契约差异，不能直接换数据库或把目标分支旧前端覆盖当前工作台。必要修复限定新建、详情与人工流程任务操作；保持新后端权限/幂等/CAS边界，不恢复退役专业API。
- 配置使用独立CWD中的config.yaml与限定环境变量即可加载，不增加配置解析机制。目标配置在受保护profiles/ga，默认8080、ga_runtime/ga_system/独立inspection，自动迁移/seed关闭；当前DEV配置文件未改。
- 复用itsm-redis-dev实例的DB12（观察为空，DEV使用DB11），独立目标附件bucket；不清空Redis，不复用DEV队列。最终启用前再次检查占用。
- 目标已有备份，archive目录可读取，但本批未声称完成该备份的恢复演练。实际权限变更需事务回滚预演，初始化依已审定向工具的plan/apply与隔离PG回归证据，保存前后计数/身份/账本/core ACL。
- P037四核心表ACL/控制收据/迁移账本保持不变；额外runtime业务ACL及execution角色绑定须经独立核对并复验。不得用owner启动应用。
- 先审配置和权限、检查启动准入，再在原端口切换。已保存当前DEV二进制摘要、argv、工作目录和环境的受保护恢复快照；失败恢复原启动，不覆盖原配置。
- 完成条件：必要兼容修复独立复审、目标启动检查、新建generic/incident/服务请求可用范围的API/UI证据及回退方式。未测到的专业操作、SLA计时及外部投递如实列差额，不算通过。

私有批次目录：`/home/administrator/.local/state/itsm-backend-switch-20260915/`。所有密码/启动环境/备份只在此保存；仓库仅记录非敏感清单。首批从09:23 CST开始，最多60分钟报告差额。


### 09:36–09:46 CST 执行证据

- 目标写入前备份 `backup/itsm_ga_ready-before-switch.pgdump`：SHA256 `9474f069b55b10d1f5183c2266536dc53bcada4b56535acbb9133d13ac6989ee`；随后实际恢复至既有隔离测试容器中的 `ga_acl_rehearsal_20260915`，用于本批权限准入演练，不是新业务目标或新部署环境。恢复成功，工单为0。
- 产品类型CLI制品 SHA256 `01657ef5eb70dd88a6c60eb22254015ffe541643d56dcab817f30627e31c145a`：plan/apply/repeat，12新建、复跑0新增/12保留，目标tickets仍0。13张身份表、迁移账本和四核心表ACL摘要不变，证据 `ticket-types-execution-result.json`。
- 定向CLI使用既有迁移操作员ga_owner，因此仅operator配置RLS_MODE=off；首次enforce被权限边界拒绝后核对源码再修正。没有关闭数据库RLS，没有改变应用profile；应用仍ga_runtime/enforce，禁止owner启动。
- 独立复核确认固定94b30926实际bootstrap仍消费Redis刷新/撤销存储。PostgreSQL鉴权存储尚未接入启动、登录、刷新、注销及重启恢复，不算验收通过；本批实际运行ACL不预授未消费auth_state表权限。
- 原DEV继续运行；尚未切换或启动目标应用。本批备份、配置初始化与权限预演不能替代真实API/UI验收。


### 09:58 CST 准入与审查更新

- 运行ACL已实际经恢复演练及目标事务回滚预演提交：ga_runtime 51个具名普通表目标（auth_state两表从草案排除）、15个序列USAGE、精确standard绑定1行；ga_system零增量。目标除新增binding外所有表数据摘要、core4目录/权限、角色、P037控制文件不变。
- 原核验辅助工具误用操作员 InspectRuntimeMigrations 导致ledger权限拒绝；已修正为应用实际 InspectRuntimeDatabase→独立ga_inspection，不给应用授予ledger/evidence读取权限。真实uid1000执行角色/执行模式/P037迁移准入通过。
- Redis DB12 PING通过且0key；MinIO独立空桶已创建。生产CORS明确允许localhost/127.0.0.1:3001。启动脚本按uid1000预检/运行、精确PID/制品确认、异常自动恢复，独立模拟5路径通过；尚未真正切换。
- 后端兼容修复源c0f022ea/546dcb93独立审查通过，集成本分支1c6d49f4/c3c880df并构建成功。真实PG12组授权投影、候选分配、CORS回归证据已保留；全suite基线故障不声称修复。
- 前端7f7862b9独立审查复现静态确认框会话变更风险及部分成功回执复用，正修复；未使用该中间制品替换现有前端。初审关于status字段不被消费的判断已被根任务核对固定94源码否定并撤回：DTO存在Status，service校验并写入，普通工单功能应保留。
- callback可触发BPMN webhook，不能仅凭webhook能力关闭断言无企业调用。所选三个默认流程须按真实ID/XML哈希独立检查后才能执行；其余云流程中发现两份既存XML格式错误，列为未验收，不在本批猜测修复。

本批环境预检修订记录：受保护 `environment-revision-preflight.json`。它明确 application_e2e_accepted=false，不能用于宣称G-B通过或切换完成。
