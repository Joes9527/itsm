# Dev 031 与主干 047 分离原因：核验结论及 Agent 接续指引

- 状态：分析核验 completed；解决设计 pending discussion。本文不是迁移、切库或数据清理授权。
- 核验日期：2026-09-16（Asia/Shanghai）；源码基线 `00779a067060eae89846e5f6844ebe8c9b18ee13`。
- 目的：解释为什么原共享 Dev 停在031而主干已有047，避免后续 Agent 把源码、测试库与运行库混为一谈。
- 本文维护原因与证据；任务状态仍由[唯一续办清单](../superpowers/plans/2026-09-15-migration-validation-ledger.md)维护，运行规则仍由[开发环境合同](../development-environment.md)维护。

## 1. 已确认结论

原共享开发基线使用 `itsm-postgres-dev / itsm_config_baseline_20260908 / public`。新迁移代码在独立开发分支推进，经隔离验证后合入主干；这些新结构没有同步到当前原 Dev。源码集成、数据库迁移及实际部署是三个独立完成条件。

047是 `047_bpmn_assignment_source` 迁移编号，不是数据库名，也不表示共有47条收据。它最初在独立功能分支编号为032；合入主干时032已被SLA迁移占用，因此改号047。不能将新迁移代码存在或隔离测试通过理解为共享Dev已升级。

这解释了当前差异，但不证明所有历史开发从未直接写过Dev，也不证明过去绝不可能有过恢复或回滚。应以当前真实账本、结构和具名历史证据作有限结论。

## 2. 可复核时间线

| 时间（北京时间） | 直接证据 | 可以得出的结论 |
| --- | --- | --- |
| 09-14主干集成前 | [历史对账记录第2.2、3节](../migrations/2026-09-14-schema-ledger-reconciliation-plan.md)：共享基线031，候选线046，当时领先主干251提交 | 当时源码线和数据库目标存在分离；251是当时快照，不是现在差距 |
| 09-14 18:39 | `5c12326d5` 新增功能分支 `032_bpmn_assignment_source.sql` | 任务负责人绑定结构变更源于独立功能开发 |
| 09-14 20:04 | PR25合并 `8f8d34fe3930f558d77aa6410326d821df559d8e`；父提交 `a25e108d2`、`467b94dd2`；与第一父比较注册了032–046 | 迁移源码进入main；不证明这些迁移全部执行，尤其038退休独立受控 |
| 09-15 10:26 | `6cbcb440efdd15b42effbe50a5bb845c31a9dec8` 相对第二父 `a6be9417`：SQL和verify文件均为R100，032改名047 | 两文件内容未变；主干032是 `032_workitem_sla_cycle`，改号有明确原因 |
| 09-15 12:16 | PR27合并 `018432df384ef19005f26232f995f9af91b2e894` | 047进入主干；合并不自动执行共享库迁移 |
| 09-15后续预演 | [唯一清单中的U1/U2预演证据](../superpowers/plans/2026-09-15-migration-validation-ledger.md#development-restoration-update) | Dev恢复副本验证P037及普通032–036、039–047；038未执行，副本归档移除；真实Dev未同步 |
| 09-16再次现场只读核验 | 查询具名Dev账本、information_schema及配置目录 | 当前Dev仍031且缺047字段，与历史未部署记录吻合 |

Git独立核验方式（只读）：

```bash
git show --format=fuller --no-patch 8f8d34fe3
git diff 8f8d34fe3^1 8f8d34fe3 -- itsm-backend/migration/
git diff --name-status 6cbcb440e^2 6cbcb440e -- itsm-backend/migrations/
git show --format=fuller --no-patch 018432df3
```

039–046的版本常量分布在 `itsm-backend/migration/` 的各功能文件，不可仅搜索注册表中的数字字面量后误判不存在。

## 3. 测试在哪里，Dev在哪里

| 对象 | 证据与边界 |
| --- | --- |
| 初期任务负责人绑定测试 | `codex-workitem-assignment-test-20260914`，端口36444，数据库`sslvpn_test`，每例独立schema；[生命周期验收计划第7.1节](../superpowers/plans/2026-09-14-catalog-lifecycle-validation.md)明确无共享数据库写入 |
| 合入后的047测试 | [任务分配报告](2026-09-15-work-item-task-assignment-report.md)记录隔离PostgreSQL的34个顶层测试通过；该段未重新点名容器，不能断言一定是上行同一个容器 |
| Dev升级预演 | `itsm-dev-upgrade-rehearsal-20260915`，同版本PG17、无网络和发布端口、tmpfs；已归档移除，不是另一个长期Dev |
| 原共享Dev | 当前24条迁移记录，最高031；`public.process_tasks.assignee_source`列不存在。此前已核实新版启动依赖的执行域表缺失，不可直接只改连接 |
| 原开发配置 | 私有状态目录`config/itsm/config.yaml`选择`itsm_config_baseline_20260908`；历史launch记录采用该目录，和历史基线报告一致 |
| 后来的运行配置 | `config/itsm-wsl-development/config.yaml`选择`itsm_ga_ready`；这是隔离目标，不是原Dev。09-16本次检查服务已停止，配置目的地不等于此刻存在活动连接 |

私有状态根为 `/home/administrator/.local/state/itsm-kaf-baseline-20260908`。核查仅输出库名等白名单元数据，禁止复制完整配置、凭据或环境变量到报告。最高版本号不足以替代完整依赖、校验和与结构检查。

## 4. 必须纠正的误解

- `itsm_migration_20260914` 当前账本最高019、14条，并非已证明来自当前031 Dev的完整克隆；其最初来源不能由名字推定。
- 五批384对象的历史证据属于 `ga-itsm-20260914 / itsm_ga_ready` 与 `gb-remediation-test-pg-20260914 / gb_replay_review`，不是旧019对照库，也不是未来新克隆的验收结果。
- “所有开发都在WSL”不等于“所有测试和迁移都写入同一个共享Dev”；同主机、同仓库和同数据库是不同身份。
- 047只增加任务负责人来源结构，不会自动修复19条旧流程绑定。824是绑定配置ID，不是结构版本或历史工单数量。
- 源码032–046全部存在，不代表数据库执行过全部迁移；R038退休必须单独处理。

## 5. 后续 Agent 如何接续

1. 先读本文，再读环境合同与唯一清单；历史报告的“当前main”和“运行中”均按其日期解释。
2. 固定源码提交、前后端制品、数据库实例/库/schema和实际运行profile，分别核验。不要依据端口、目录名、GA字样或最大迁移号猜环境。
3. 已确认业务目标保持不变：同一套新代码，日常Dev与可追溯Dev克隆验证；3010→8080；复用现有迁移工具；旧ITSM ticket及历史流程数据不迁。
4. 047是当前已选代码的结构要求；具体采用原库前向升级还是其它保留数据的恢复路径，正在brainstorming讨论。上一版“先升级Dev再建克隆”的建议不是已批准执行设计。
5. 维护者在本轮讨论中进一步确认：允许整理或重建测试数据，但身份和业务配置必须保留。实例7、8、9、10已明确废弃；其它对象仍须先分类。该确认不等于允许清除账号、组织、权限、19条绑定、自定义流程824或云业务配置；配置内容保留与其如何适配新模型需在设计中分别明确。
6. 讨论应明确Dev数据保留范围、共享入口可用性要求、旧配置处置及克隆刷新边界；批准设计后再形成实施计划。不要在讨论中先执行升级、覆盖、清理、停用绑定或启动消费者。
7. 验收分层：规范迁移与权限、数据保全、readiness、登录、代表性UI业务路径分别记录；隔离测试与代码合并不能替代共享Dev交付。

## 6. 本次核验限制

本轮没有启动已停止的ga/replay容器，未重新验证其历史046结构或384对象当前状态；没有查询每一个历史临时库。旧二进制的完整构建来源和每次历史连接切换时间未全部重建。上述缺口不改变当前Dev缺少新结构及047源码来源的直接证据，但不得扩展成对全部历史操作的保证。
