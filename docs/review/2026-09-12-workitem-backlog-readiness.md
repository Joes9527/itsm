# WorkItem 后续 backlog — 就绪评估（归档，不实现）

> 日期：2026-09-12。状态：**全部未实现**。本文件只做现状、风险、依赖、验收标准与所需授权的归档；
> 不把 backlog 伪装成已修复，不据此开启新修复波次。任何实现都需要单独设计、授权、隔离验证与独立复审。

| ID / 项 | 为什么是 backlog | 风险与影响 | 依赖 | 验收标准（未来） | 所需授权 | 下一步 |
|---|---|---|---|---|---|---|
| `BL-WI-PROCESS-AUDIT-CONTINUITY`（I2） | P 对 P 前既存 `process_instances/process_tasks/process_callback_outboxes` 使用整行摘要；观察期正常推进会导致 R 严格拒绝 | 一般在途流程退役能力未验收；不得据此宣称支持所有 P 前活动记录的观察期进展 | 领域所有者、事务、租户、权限、审计边界内的可验证原始行身份与冻结载荷设计 | 真实创建 P 前 process/task/入队 callback → P 后经所属运行入口推进 → 最终恢复点 → R 成功；缺失/错租户/错身份/伪造/断裂证据、冻结载荷变动、行消失仍拒绝；事务失败回滚与幂等重试无重复效果 | 维护者范围批准 | 先出设计，不实现 |
| `BL-WI-PURGE-AUDIT-01` | 物理清理与可靠事务审计未定义；当前只支持领域软删除 | 物理消失无法作为原子清理/已消失扩展的身份凭据；HTTP 审计在业务事务后写入 | 所属领域清理入口、原始记录/WorkItem/租户身份、保留值范围与摘要、动作/执行者/时间、唯一操作身份、同事务不可变结果 | 所属领域清理入口可审计；P/R 保留值、身份与恢复演练通过；伪造/自签声明、日志替代事务审计均拒绝 | 单独设计与批准 | 出设计草案，不实现 |
| `BL-CHG-WO-01` | Change 多 WorkOrder 执行是后续功能决策 | 当前实现不覆盖多 WorkOrder 并行/分派/回滚 | Change 生命周期、任务模型、BPMN 拓扑、权限与审计 | 多 WorkOrder 创建/执行/取消/回滚/幂等/权限/审计均有真实 PG + 前端旅程 | 产品与架构批准 | 需求澄清后再设计 |
| `BL-RESP-01` | 首响测量语义未定义（assignment/start 不应隐式计为 response） | 可能污染 SLA 响应指标 | Incident 生命周期、SLA 记录、审计事件 | 首响定义、记录时点、重开/重派/多响应去重、查询/报表口径明确并回归 | 产品与运维批准 | 口径评审 |
| 未注册独立 RCA SQL | `20260909_problem_rca_authority.sql` 未注册普通迁移，含 backfill/drop；不得伪造 033 回执 | 独立 RCA 路由可能不可部署；直接执行有历史数据风险 | RCA 数据模型、backfill 范围、03x 编号、迁移目录注册与 checksum | 单独迁移设计、真实数据 backfill/reconcile、回滚/恢复演练、RCA 路由端到端验收 | 维护者单独批准 | 设计/注册评审，不执行 |
| HMAC provider secret/nonce 恢复 | 隔离恢复只验证显式空 provider 且真实拒绝交换 | 启用 HMAC provider 的 secret/nonce 恢复未验证；恢复后重放风险未知 | provider 配置、secret 存储、nonce/消费记录、恢复合并策略 | 旧/恢复点后 secret 与 nonce 行为可验证；重放拒绝；用户无需伪造成功 | 安全与运维批准 | 设计威胁模型 |
| Redis 冷启动撤销降级加固 | 认证在 Redis 冷启动失败时可能使用空内存撤销存储 | 极端情况下 access 撤销可能短暂不可用 | 认证/撤销存储、Redis 可用性、降级策略与告警 | 冷启动失败时 fail-closed 或可验证降级；撤销语义与审计明确 | 安全批准 | 设计 + 故障注入 |
| 早期无法归属的匿名卷 | 历史探索卷归属不明 | 低影响残留不确定性；不得猜删/prune | 资源清单与当时日志 | 归属可证后按精确 ID 清理；否则长期披露 | 维护者确认 | 保留披露 |

## 结论

- 以上各项均不阻塞当前 `c6144d70` 范围的修复、隔离 V1 与复审收口。
- 其中 I2 是受控退役能力的主要剩余限制；其余为功能/运维/安全加固 backlog。
- 在获得各自授权前不实现、不改写历史证据、不进行真实目标操作。
