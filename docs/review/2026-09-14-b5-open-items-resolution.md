# B5 未决项处置：20 个未解析用户 ID 与 14 个孤立 ctiId

- 状态：**已分析并给出处置建议（不写目标）**。日期：2026-09-14。
- 目标：`ga-itsm-20260914 / itsm_ga_ready`，租户 `tenant_id=1`。目标 `ticket_assignment_rules` 仍为 0 行。
- 依据：`cti_authorized.json`、`cti_tree.json`（82 节点）、`sys_user.json`（12,387 行全量）、`sys_role.json`/`sys_group.json`、KAF 只读客户端探测。

## 1. 20 个未解析用户 ID

| 检查 | 结果 |
| --- | --- |
| 在 `sys_user`（全量 12,387 行，`total=12387` 已拉全） | **不存在**（0 命中，任何字段） |
| 在 `sys_role`（28）/ `sys_group`（31） | **不存在** |
| 公司范围 | 与已解析用户**同一** `companykey=2016082500001`；1 条属 `companyId=6356b6a9…`（第二公司），非"跨租户拉取不全"问题 |
| 按 id 过滤探测 `/sysUser/list.do?userId=…` | **该端点忽略过滤参数**（仍返回全量前 5、`total=12387`）→ 无法用过滤器确认，结论以全量比对为准 |

**结论**：这 20 个 `authorizedId` 指向**已删除/已不存在的用户**。其对应路由**无有效受理人** → 建议**登记为失效（void），不迁移**。

## 2. 14 个孤立 ctiId（118 条路由）

| 检查 | 结果 |
| --- | --- |
| 在旧 CTI 树（`cti_tree.json` 82 节点） | **不存在** |
| `/cTI/list.do?ctiId=…` 探测 | 返回 `rows=0 total=0`（该端点亦可能忽略过滤，但全量树已无此 id） |
| 路由特征 | 分布在 module 1/3 与知识流程；`authorizedType` 以 2 为主、含角色 0；`definitionId` 集中在 `orderRequest:31:5565134`、`sj002:22:5565130`、`knowledgeProcess:15:5867051` |

**结论**：这些是**已删除的 CTI 分类节点**（不在现有树中）。目标无对应分类/CI → 建议**登记为失效（void），不迁移**。

## 3. 对 B5 可表达子集的影响

- 落在已映射分类上的路由：**14 条**；其中 `authorizedId` 可解析的只有 **9 条**（5 个不同用户）。
- 其余 **5 条**指向上述未解析用户 → 随之失效。
- 因此 B5 在补齐"旧→新用户 id 映射"后，**实际可写上限为 9 条**（且仍需角色动作/维度扩展才能覆盖余下 706 条）。

## 4. 处置（**已确认**，2026-09-14）

| 对象 | 处置 | 影响 |
| --- | --- | --- |
| 20 个未解析用户 id（68 条路由） | **void（已确认）**，登记删除，后续批次不得再纳入 | 路由失效，不写目标 |
| 14 个孤立 ctiId（118 条路由） | **void（已确认）**，登记删除，后续批次不得再纳入 | 路由失效，不写目标 |
| 其余 720−68−118 条 | 仍需维度扩展/身份映射后方可评估 | 见 `b5-routing-dry-run.md` §4 |

> 本文件不写入任何目标行；两条 void 已并入操作手册 §7.1 删除/排除登记。
