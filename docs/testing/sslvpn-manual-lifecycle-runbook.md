# UI 全流程测试手册：SSLVPN、邮件建单与自动回复

状态：UI 验收手册，2026-09-14 修订。按用户确认，**本轮通过 ITSM/KAF 页面和真实邮件客户端操作，不做 API 验收，不以接口或数据库结果替代 UI 通过**。ITSM 源码参考 `a25e108d2`，KAF 本地参考 `dfe304da`；本次为文档及静态核对，没有执行 WSL 测试或发信。

给 Coding Agent 执行前，先读[执行说明](sslvpn-coding-agent-execution-guide.md)，复制[执行记录模板](sslvpn-agent-run-record-template.md)。详细邮件步骤见[邮件建单与自动回复 UI 手册](email-ticket-ui-runbook.md)，它是本轮必测部分。

具体账号候选、角色/审批组、菜单路径及 WSL 入口见[账号与菜单清单](sslvpn-ui-accounts-and-menus.md)。历史账号必须先在本轮环境确认有效。

## 1. 范围与执行路线

两条路线共享工单查询、协作和生命周期检查，但不强行合成同一个专业流程：

- **SSLVPN 路线**：管理页面配置字段/流程 → ITSM 表单或 KAF 对话发起 → 两级审批 → KAF 履约 → 页面查看结果 → 关闭能力检查 → 历史/权限恢复检查。
- **邮件路线**：真实邮箱发信 → ITSM 自动建单 → 邮箱收到自动确认信 → 回复原邮件 → 原工单追加内容 → 工程师处理及通知 → 关闭能力检查。

当前邮件入口创建通用工单；不会因为主题写了 SSLVPN 就自动成为目录服务请求或获得授权。SSLVPN 是 `service_request_item`，邮件工单是 `generic`。前者的一个专业扩展与一个 WorkItem 共同创建，不另建第二张 Ticket 接续审批。

### 1.1 UI 验收规则

1. 人工或 Agent 通过可见页面点击、输入、上传、下载、搜索和刷新。浏览器自动化可以操作真实控件；不得直接调用后端接口、执行浏览器脚本发请求、注入登录态、改表或调用 Graph 来代替用户动作。
2. 邮件必须从实际测试邮箱客户端（Outlook 网页/桌面或其他已确认客户端）发送、回复与接收。不能以模拟发信、接口回执或日志中的 sent 作为邮件送达证明。
3. 页面缺少必需操作：记录 BLOCKED；页面行为/内容错误：记录 FAIL。可以在环境负责人已准备好的独立配置上继续其他 UI 用例，但不能把原配置用例补记 PASS。
4. 网络请求、日志、数据库与运维命令可由有权负责人另行排障；排障修复后必须回到 UI 重测。本手册没有 API 补充通过路径。
5. 部署是独立前置工作。保留[WSL/KAF 部署参考](../deployment/sslvpn-wsl-deployment-and-manual-verification.md#wsl-dev-entry)，测试员从负责人交付的可用环境开始，不因阅读该文档自动执行部署。
6. PASS 必须有本轮页面/邮箱证据。未执行记 NOT RUN；依赖不齐记 BLOCKED；不适用需写理由；有功能缺口不能标 N/A 来规避。

## 2. 准备环境、身份与证据

### 2.1 开始前登记

| 项目 | 本轮记录 |
|---|---|
| 测试轮次、日期、时区、执行人 | 每个标题使用唯一标记，如 SSLVPN-UI-日期-序号、MAIL-UI-日期-序号 |
| 访问入口 | ITSM、KAF、真实邮箱客户端、必要时授权目录管理门户的实际 URL |
| 版本与环境 | 负责人交付的 WSL 运行版本和准备结论；当前页面显示的版本若有则截图 |
| 专用测试对象 | 目录名称/编号、流程 key/版本、测试邮箱收发地址、指定 Dev 授权用户/组的受限引用 |
| 邮件观察窗口 | 按实际轮询及通知配置登记等待期限；超时后记录，不反复发新邮件 |
| 权限恢复 | 本轮新增权限的恢复负责人、允许的 UI 入口或独立运维交接方式 |
| 证据 | 本轮私有目录、截图/邮箱邮件引用、工单编号与详情 URL，不记录密码/token |

历史日常端口是 ITSM 3001/8080、KAF 5173/8000，不能直接当本轮实测；由环境负责人确认实际入口。仅登录页可打开不代表登录、业务处理或邮件已就绪。

### 2.2 角色与窗口

| 角色 | 用途 |
|---|---|
| A 管理员 | 目录/字段/流程/审批链/连接器页面配置 |
| R 申请人 | 普通 ITSM/KAF 用户，测试邮箱发件人；邮箱地址对应本租户 active 用户 |
| M 主管 | 主管审批；候选组 dept_manager |
| N 网络运维 | 二级审批；候选组 network_eng |
| E 工程师 | 分派、处理、评论、附件、通知及允许的关闭动作 |
| O 环境/恢复负责人 | 独立部署前置与恢复交接，不把后台操作计入 UI 验收 |

不同角色使用独立浏览器配置文件，进入系统先确认当前账号/租户。不能用管理员代替所有角色；两个普通无痕窗口可能共享身份。审批组成员与同名角色需分别在管理页面核对。

### 2.3 留证

每步保存“角色、时间、页面路径、工单编号、操作、可见结果、截图引用”。邮件留发件箱和收件箱的收发时间、收发人、主题及必要正文截图。敏感内容去敏，不把完整邮箱导出或凭据页截图上传仓库。

页面不展示的信息标记“UI 不可验证”；不能凭隐藏字段猜测。页面结果与后台排障结果分别记录。

## 3. 环境 UI 就绪检查（D01–D06）

| 用例 | 操作 | UI 预期与放行条件 |
|---|---|---|
| D01 | R 打开实际 ITSM/KAF 地址，A 检查负责人交付的版本记录 | 目标环境明确，不混用历史 C4 实例 |
| D02 | 需要部署时交 O 按独立部署说明准备；登记交付时间/版本，重新打开页面 | 记“环境前置已交付”，不计作 UI 部署能力通过 |
| D03 | R 正常登录 ITSM 和 KAF，进入目录/工单及对话页面 | 身份、租户正确，没有加载失败；不注入 JWT |
| D04 | A 在用户/组管理页核对 M/N；R 在 KAF 目标 workspace 正常访问 | 角色与成员可确认；隐藏配置由 O 交付，页面缺管理入口单独记缺口 |
| D05 | R 新建 KAF 会话询问本轮 SSLVPN 服务，暂不确认 | 能找到正确目录和期限，确认前不出现新工单或“已开通” |
| D06 | O 交付消费者、旧任务隔离、固定 Dev 对象基线和恢复安排；A/R 查看可用的状态页面 | 准备记录齐全；UI 不展示队列/成员关系时，记录 UI 观察边界，不用健康接口判 UI PASS |

邮件路线另完成邮件手册 M01。某一路线前置被阻塞，可独立执行另一条已授权路线。

<a id="form-configuration"></a>

## 4. 自定义字段表单（F01–F09）

A 打开 `/admin/service-catalogs` → “新建服务目录”，填写本轮专用名称、描述、分类；交付时间填数字 `1`（天），不要照 placeholder 输入“1-3个工作日”。工作项类型选“服务请求”、需要审批选“是”、状态先“禁用”。

在“自定义字段”点击“添加自定义字段”，依次配置：

| 字段标识 | 展示标签 | 类型 | 必填 | 测试输入 |
|---|---|---|---|---|
| office_location | 办公地点 | Text | 是 | 上海 |
| business_context | 远程办公用途 | Textarea | 是 | 本轮测试，远程访问已批准内部服务 |
| device_count | 设备数量 | Number | 是 | 1 |
| planned_use_date | 计划使用日期 | Date | 否 | 执行日后的一个日期 |
| access_duration | 授权期限 | Select | 是 | 负责人已准备的期限选项 |

现有编辑器支持这五类以及标识/标签/必填/Select 选项，顺序按当前列表保存。没有默认值、placeholder、条件显隐、正则、数字范围及拖拽排序的完整配置入口，不编造操作按钮。

**SSLVPN 配置缺口**：当前目录管理 UI 不完整支持 `serviceType=access` 和 accessPolicy；Select 输入框使用逗号分隔，不能完整表达编码 `30d` 与标签“30天”的独立配置。将完整 SSLVPN 策略配置记录为 UI BLOCKED，交 O 预先提供已验证测试目录。本轮不调用 API 补齐并宣称 UI 配置通过。可用普通禁用测试目录检查字段编辑器，用已准备 SSLVPN 目录检查申请到履约；分别登记两个目录，不能混淆证据。

| 用例 | 页面操作 | 预期 |
|---|---|---|
| F01 | 保存字段，刷新列表并重新编辑 | 五类字段、标签、必填、选项与顺序保持 |
| F02 | 在专用目录尝试空标识、空标签、重复标识再保存 | 明确拦截，无覆盖/字段丢失；错误保存记 FAIL |
| F03 | 查看已准备 SSLVPN 目录及可见配置，重新编辑保存前后比较 | 可见审批开关、类型、流程、选项未被无意改写；不可配置/不可见项记 UI 缺口，不点保存猜测补齐 |
| F04 | R 在目录打开申请页，查看“该服务的补充信息” | 五种控件、标签、必填及顺序正确，无其他目录字段 |
| F05 | 先填基础联系人/邮箱/标题/理由，再逐个留空必填字段点击提交；另试纯空白文本 | 提示具体字段；页面列表无新申请，不能只看按钮是否禁用 |
| F06 | 数字输入 1，再尝试非数字；期限选择合法选项 | 正常值可提交；非法输入不能成功。只报告 UI 输入约束，不伪造非法请求测试服务端 |
| F07 | 分别留空/填写计划日期提交拒绝测试申请 | 选填不阻塞；日期保存展示无可见偏移；日期不是授权到期时间 |
| F08 | 提交后打开统一工单详情“业务扩展参数”，刷新并重新登录 | 原标签和值保留；Select 可能显示原编码，若理解困难记录显示问题 |
| F09 | R/M/N/E 按各自权限读取原工单，审批和履约后再读 | 有权可见且原答案不变，无权拒绝；字段未在页面回显就是 UI FAIL |

基础申请字段不重复添加为自定义字段。F07/F08 复用拒绝用例，无需额外触发真实授权。

## 5. 流程引擎与审批链（W、AC）

### 5.1 配置与版本

| 用例 | UI 操作 | 预期/缺口处理 |
|---|---|---|
| W01 | A 在 `/admin/workflows` 和设计器查看本轮 SSLVPN 模板，核对主管→网络运维→授权服务任务及两条拒绝分支 | 节点可查看；若不能通过 UI 完整配置/发布 policy 引用和服务任务属性，记 UI BLOCKED，交 O 提供已发布版本，不改为调用发布接口 |
| W02 | `/workflow/versions` 选择正确 key，查看本轮版本，在授权窗口内“激活”；回到目录绑定“专属审批流程” | 版本和绑定页面显示一致；此页面当前主要用于查看/激活，不能假设有新版本 XML 上传按钮 |
| W03 | 创建申请后在 `/workflow/instances` 打开对应实例 | 可关联同一工单，当前节点和实际版本可查；缺失必要展示记问题 |
| W04 | 分别观察一级前、一级通过后、拒绝后的 M/N 待办 | 顺序正确，拒绝后不能继续正常授权 |
| W05 | 独立窗口内用 V1 创建拒绝测试申请，再激活已准备 V2 创建另一条 | 旧实例 V1、新实例 V2；无发布管理 UI 则记 BLOCKED，未安排窗口记 NOT RUN |

固定模板是 `sslvpn_approval_flow`。主管组为 `dept_manager`，网络组为 `network_eng`；实际执行以当前已发布 BPMN 为准。`/workflow/ticket-approval` 是设计器，人工审批用 `/approvals`。

### 5.2 审批链显示与执行（AC01–AC05）

后台 `/admin/approval-chains` 配置、工单“审批链（预解析）”以及实际 BPMN 决策是三个不同内容。当前 SR 创建仍读取租户服务请求 active 审批链；固定 SSLVPN 双级模板并不靠它动态生成节点，不要求重复建立审批链。

| 用例 | UI 操作 | 预期 |
|---|---|---|
| AC01 | A 查看本租户服务请求审批链配置及状态，与本轮流程图比较 | 记录无配置、唯一配置或多配置；不凭列表排序猜生效项，不改共享规则 |
| AC02 | R 新建后打开“审批链”页签；M 查看审批中心，A 查看实例 | 没有决策不等于没有流程。若详情显示“未走审批流程”但存在待办，记 UI FAIL |
| AC03 | M/N 逐级决定后刷新审批链页签 | 实际节点、审批人、意见和时间正确；预解析角色不冒充实际审批人 |
| AC04 | 比较预解析卡片与真实流程/决策 | 差异明确登记，无预解析卡片不自动失败，但真实待办/决策必须可查 |
| AC05 | 独立测试租户中通过管理 UI 配置并实际运行金额、组织、会签/或签场景 | 保存成功不足以证明执行；功能缺 UI 或不生效记 BLOCKED/FAIL；本轮未选扩展记 NOT RUN |

## 6. SSLVPN 两个受理入口与拒绝（R01–R04）

### 6.1 ITSM 表单、主管拒绝

1. **R01.1 / R**：在本轮目录完成字段校验，标题含 FORM-REJECT，点击一次“提交申请”。记录回执编号和详情链接；提交结果不明时停留原申请，不新建替代。
2. **R01.2 / R**：从 `/tickets` 清除旧筛选，查原编号，核对申请人、五类字段和待审批状态；不应出现“已授权”。
3. **R01.3 / M**：在 `/approvals` 按“业务单据”编号和流程 key 找“上级领导初审”，打开关联详情核对原表单，再返回“领取”。
4. **R01.4 / M**：点“拒绝”，先留空意见，确认被拦截；填写“本轮 UI 测试：主管拒绝”后提交。
5. **R01.5 / R/N/A**：刷新原详情、二级待办和流程实例。应显示拒绝/拒绝终点，无二级可办任务和授权完成展示；原字段和拒绝意见保留。

审批中心当前只取前 100 条任务再筛待办，找不到时先使用页面可用筛选/刷新和工单实例入口。仍找不到记 UI 可发现性缺陷，交负责人排障，不通过任务接口取 ID 后代批。

### 6.2 KAF 对话、网络运维拒绝

1. **R02 / R**：在目标 workspace 新会话申请本轮目录，提供五类字段。核对确认卡、身份、用途、期限，确认一次；打开 ITSM 详情核对同编号和字段。KAF 不能收集/展示必填内容时记 UI 缺口，不用隐藏请求补值。
2. **R03.1 / M**：在审批中心核对关联原单，领取并批准，填写可识别意见；随后 N 才应看到二级任务。
3. **R03.2 / N**：打开原表单核对，领取并拒绝，填“本轮 UI 测试：网络复审拒绝”。
4. **R04 / R**：原 KAF 卡刷新，离开并重开会话；ITSM 原详情刷新。两端显示同一申请拒绝，无“权限开通成功”。

拒绝流程实例可能技术上显示“已完成”，不能把这个词当成授权成功。没有 UI 能观察外部成员关系时，外部无授权这一项记录“UI 不可验证”，不以后台日志补记 UI PASS。

## 7. 正常履约与结果（P01–P07）

只有本轮授权、Dev 对象基线和恢复安排明确后，才能做最终批准。FORM 和 KAF 两条正向路线串行执行；每次按第 11 节恢复后再开始另一次，或使用各自明确批准的独立对象。

| 用例 | 页面操作 | 可见验收结果 |
|---|---|---|
| P01 | R 通过表单或 KAF 确认一次，分别标记 FORM-PASS/KAF-PASS | 产生一个新编号和正确详情，保留字段 |
| P02 | E 在允许阶段“分派”、添加公开评论、上传小文本附件，下载后打开 | 协作可见、内容一致；分派不改变审批角色 |
| P03 | M 打开关联详情、领取并批准 | 出现网络二级待办，尚无成功授权结果 |
| P04 | N 核对并最终批准，观察实例与专业面板 | 进入授权服务任务/履约中；不能手点“开始交付”代替 KAF |
| P05 | 在 UI 可观察时记录履约中→完成的过程；过快则记录未观察到中间态 | 不为截屏停 Worker/打断真实授权；持续未完成按观察窗口记录 FAIL/BLOCKED |
| P06 | R 查看“服务申请与规格参数”，A 查看流程实例 | 专业“已完成”、授权验证结果/验证时间/有效期可见，任务及流程到正确终点；缺信息单列问题 |
| P07 | 多次刷新；KAF 入口在原卡刷新和重开会话；查看原字段和审批记录 | 编号、原答案、验证时间/有效期一致，列表无重复记录；不声称页面刷新已经证明协议幂等 |

ITSM 表单创建的申请不强求存在原 KAF 对话卡。若结果显示“权限已存在”，单列已有权限分支，不当成本轮新增授权通过。

如本轮要求 UI 证明实际组授权，由已授权人员使用 Entra 管理门户等真实目录管理 UI 查看固定组成员并留前后证据；没有可用管理 UI/读取权限时，外部效果项记 BLOCKED，不用 Graph 调用替代。本轮不验证 VPN 登录或网络连通，也不承诺到期自动撤权。

## 8. Ticket 生命周期与关闭（L01–L06）

专业“已完成”、共享已解决和已关闭不同。当前 SSLVPN 模板无额外用户确认关闭节点，详情也无独立关闭/重开按钮；本轮要测出这项 UI 缺口，不能改用接口把工单关掉。

| 用例 | UI 操作 | 预期 |
|---|---|---|
| L01 | 完成后刷新详情、审批链、历史、字段和附件 | 原数据及结果可追溯，不把专业完成写成已关闭 |
| L02 | 检查真实页面的正式关闭动作和允许角色 | 没有动作记 UI BLOCKED；有动作则只在无活动履约/unknown 后进入 L03 |
| L03 | 使用本版本已提供的正式关闭 UI，填写反馈并确认，再查看状态与历史 | 已关闭、关闭时间/操作记录可见，字段和授权结果保留；没有 UI 则 BLOCKED，绝不使用状态编辑下拉框绕过专业流程 |
| L04 | 在已有独立 closed 测试记录上检查动作和只读提示 | 当前 closed 为终态；有重开按钮时记录与领域约束是否冲突，不对原授权单尝试重新授权；无按钮不是接口拦截的证明 |
| L05 | 新建未最终批准申请，申请人查找正式撤回/取消动作并按 UI 操作 | 若缺入口记 BLOCKED；若可取消，待办、流程、状态一致；申请表“取消”返回目录不算撤销已提交记录 |
| L06 | A 在专用未批准实例点击“终止”，核对确认框后确认 | 管理员终止与申请人取消分开记录；待办不可继续、专业展示与历史一致 |

SLA 在页面核对绑定策略、剩余时长、提醒和终止计时；不修改时钟/数据库造超时。只有完成长时观察才记超时用例 PASS。通知通过通知页及真实接收端核对；评论保存不自动等于邮件已发送。

## 9. 表单版本、查找和异常

### 9.1 表单版本（V01–V04）

| 用例 | UI 操作 | 预期 |
|---|---|---|
| V01 | 已提交拒绝测试申请后，在专用目录把“办公地点”改为“办公城市”；再开旧单/新表单 | 旧标签/答案保持快照，新表单用新定义 |
| V02 | R 打开表单填好不提交，A 改字段保存，R 再提交旧页 | 页面明确要求重新确认，列表没有半成品或重复申请 |
| V03 | R 点击“重新读取目录” | 兼容答案保留，重新确认后才提交 |
| V04 | 对独立测试目录删除字段或改类型，旧页重载时检查提示 | 显示不兼容答案；点击“确认舍弃上述不兼容答案并重新填写”后继续；原已提交记录不被抹除 |

在 KAF 原确认卡也检查目录变化后的重新确认；不能在开发者工具中改旧卡数据。管理员 UI 无法正确保存时暂停相关用例。

### 9.2 查找（Q01–Q05）

| 用例 | UI 操作 | 预期 |
|---|---|---|
| Q01 | 服务目录按本轮名称搜索，清除条件，查看禁用/无权条目 | 正确条目可发现；不可申请条目不能成功申请 |
| Q02 | 工单列表清除历史筛选，分别按准确编号/唯一标题查，再组合状态筛选 | 查到同一 WorkItem；不因旧过滤器误判丢单 |
| Q03 | 服务请求列表点本申请 | 正确跳转统一工单详情，编号/字段一致 |
| Q04 | 审批中心与流程实例页面定位本单 | 可发现准确待办与实例；页面分页/关联链接问题记 FAIL |
| Q05 | 查看审批/历史/通知；另用已准备的无权测试账号访问原详情链接 | 审计可见、有权可读，无权不泄露；不通过修改请求参数扩大探测 |

不预设支持按任意自定义字段搜索，缺少筛选器应记能力缺口。

### 9.3 异常（X01–X07）

| 用例 | UI 操作及边界 | 预期 |
|---|---|---|
| X01 | 独立拒绝申请连续点击提交，或使用原页面提供的同次重试 | 可见仅一条申请/同一结果；不创建新卡模拟重放 |
| X02 | 用非审批人测试账号打开审批中心和原单 | 没有可执行的越权审批；若出现允许动作仅在授权负向对象上点按验证，不调用决策接口 |
| X03 | 环境负责人独立准备消费者暂停场景时，测试员只观察 UI；本轮不主动停共享 Worker | 等待期间不假成功，环境恢复后原单继续；未准备该场景记 NOT RUN |
| X04 | 尝试从 UI 保存缺必要配置的专用目录或提交已变更目录 | 页面明确拒绝，不绕过审批；缺入口按 UI BLOCKED |
| X05 | 仅在 UI 能配置的专用无外部权限流程中验证未知任务 | 发布拒绝或执行可见阻塞，不能静默跳过；不手工改 XML/接口造测试 |
| X06 | 自然出现“结果未知”时保存原详情、时间、任务可见信息 | 不显示授权完成，不通过重试/新申请再次授权；交 O 处理 |
| X07 | 检查是否有明确、安全的原回执恢复 UI | 没有入口记 UI BLOCKED；仅刷新页面不算回执重放，删除原 API 重放步骤 |

## 10. 邮件建单与自动回复（M01–M12，必测）

按[邮件 UI 手册](email-ticket-ui-runbook.md)执行全部步骤，使用真实邮箱 UI：连接器可见配置 → 发带附件的测试邮件 → 列表发现工单 → 收到自动确认信 → 回复原确认信 → 原工单出现回复 → 工程师协作/通知 → 关闭能力检查 → 收尾。

邮件正文不是自定义表单答案。发送“办公地点=上海”不等于系统已把它映射到结构化字段；不能用邮件创建的 generic 工单冒充 SSLVPN Requested Item。

## 11. 收尾与恢复（C01–C04）

| 用例 | 操作 | 证据 |
|---|---|---|
| C01 | 用获准目录管理 UI 核对固定对象与本轮权限前后状态；核对本轮邮件/工单清单 | 区分本轮新增权限、已有权限和 unknown；有来源记录 |
| C02 | 由获准恢复人在已确认的权限管理 UI 移除仅属于本轮的新增权限，再刷新成员列表 | UI 操作与移除后截图；如只能运维恢复，交 O 独立执行并记录“运维恢复”，UI 恢复用例仍 BLOCKED，不重复删除 |
| C03 | A 通过管理 UI 禁用专用测试目录或恢复原流程绑定，确认不会影响运行实例；邮件连接器若共享不随意停用 | 配置前后截图；原工单、评论、附件和邮件历史保留 |
| C04 | 重新登录查看历史与本轮遗留项 | 无未交接的活动/unknown 任务；权限恢复不改写原成功结果；收件箱无持续回复风暴 |

正向 grant 前必须明确恢复负责人。出现 unknown 也需要核对可能已经产生的权限；不能删除无归属的已有权限。

## 12. UI 测试记录与结论

从[本轮记录模板](sslvpn-agent-run-record-template.md)逐用例记录。每项至少有“角色、页面/邮箱、操作、预期、实际、结果、截图/邮件引用、问题负责人”。

总结分别报告：环境前置；目录/字段 UI；审批链/BPMN UI；FORM/KAF 两个受理入口；履约展示与可观察外部效果；邮件建单/自动回复/回复归单/附件；工单关闭/取消；恢复。不能将部署成功、后台排障通过或接口响应合并成 UI 通过。

必需用例被阻塞时，结论写“已完成部分 UI 验证，以下步骤被阻塞”，列明缺入口或缺配置。明确“未通过 UI 完成”比绕过页面跑通更符合本轮目标。

## 13. 文档依据

- [Agent 执行说明](sslvpn-coding-agent-execution-guide.md)、[邮件 UI 手册](email-ticket-ui-runbook.md)、[部署参考](../deployment/sslvpn-wsl-deployment-and-manual-verification.md)。
- 源码静态核对：[目录字段编辑器](../../itsm-frontend/src/components/common/CustomFieldsEditor.tsx)、[统一工单详情](../../itsm-frontend/src/components/ticket/TicketDetail.tsx)、[审批记录](../../itsm-frontend/src/components/ticket/ProcessApprovalDecisionCards.tsx)、[SSLVPN 模板](../../itsm-backend/service/bpmn/sslvpn_approval_flow.bpmn)。
- [Ticket 用例](test-cases/TC-TICKET.md)、[服务请求用例](test-cases/TC-SERVICE-REQUEST.md)、[服务目录用例](test-cases/TC-SERVICE-CATALOG.md)、[流程用例](test-cases/TC-WORKFLOW.md)保留历史模块覆盖；其中 API 用例不属于本轮 UI 验收。
