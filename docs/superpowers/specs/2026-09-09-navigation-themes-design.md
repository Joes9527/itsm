# ITSM 顶栏与侧栏双主题设计

- 状态：draft（视觉方向已批准，本文待用户审阅）
- 日期：2026-09-09
- 范围：顶部导航栏、侧边栏及其直接关联的布局尺寸与主题偏好
- 依据：用户选择视觉方案 A + C，并确认采用预览字体、字号；默认 A，可切换 C。
- 代码基线：`a25e108d2`；设计分支：`codex/feat/navigation-themes`

## 1. 目标与范围

将现有固定深灰导航、白色胶囊控件、各自定义的字体和间距统一为两套导航配色：A 统一浅色、C 统一深色框架。两套主题共享结构、尺寸、排版和交互，切换时不产生布局位移。

本次为第一阶段，聚焦顶栏与侧栏。页面背景、业务表格、卡片和业务按钮的统一设计已按用户要求列入后续阶段（见第 9 节），不属于本阶段实施与验收范围。已有全局主题切换对这些区域的影响继续沿用现有行为；C 不是“全站所有业务页面完成深色适配”的承诺。预览里的页面正文、模拟工单数据和菜单分组仅用于视觉衬托，不成为新增产品功能。

不改变后端接口、数据库、领域规则、RBAC、租户边界、菜单权限、工作台权限和路由。动态菜单仍由后端提供；不得将预览里的示例菜单硬编码进产品。

## 2. 已选方案与取舍

- A（默认）：白色顶栏和侧栏，细边界、深灰文字、浅橙选中背景。视觉轻，适合长时间工作。
- C：深灰顶栏和侧栏连成整体，浅色文字、透明浅色交互背景、暖橙选中强调。保留深色气质，消除白色控件突兀感。
- B（深侧栏、浅顶栏）不作为第三种主题实现，避免增加无需求的主题配置。

两套导航都取消底部橙色流光线、强阴影和大面积白色胶囊。保留真实品牌标识，不把预览的占位“IT”图标替换成正式 Logo。

## 3. 字体、尺寸与配色

字体栈与预览一致：`Inter, -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif`。预览没有下载 Inter，因此实际字体由本机可用字体决定；正式导航沿用同一规则，不增加运行时外部字体请求。不同操作系统中文字形可能不同。

| 项目 | 规格 |
| --- | --- |
| 顶栏 / 侧栏品牌区高度 | 60px，二者底边对齐 |
| 展开侧栏宽度 | 224px |
| 菜单文字 / 顶栏主要文字 | 13px，常规 400，选中或强调 600 |
| 已有导航分组标题 | 10px，仅用于辅助分组，不承载主要操作 |
| 品牌文字 | 17px，600 |
| 搜索提示 | 12px |
| 辅助文字 | 11–12px；快捷键提示 10px |
| 菜单行 | 40px，左右内距 12px，图文间距 12px |
| 图标 | 17px，线宽约 1.6，统一风格 |
| 顶栏控件 | 32px 高，图标按钮有效点击区域至少 32px |
| 头像 | 27px 圆形 |
| 常规圆角 | 6px |
| 顶栏水平内距 | 桌面 24px，窄屏 16px |

| 导航语义颜色 | A | C |
| --- | --- | --- |
| 导航背景 | `#FFFFFF` | `#252A30` |
| 主要文字 | `#354153` | `#D2D9E2` |
| 顶栏文字 | `#354153` | `#E3E7EE` |
| 辅助文字 | `#667181` | `#A6AFBA` |
| 边界 | `#E7E9EE` | `rgba(255,255,255,0.07)` |
| 选中背景 | `#FFF1E8` | `rgba(255,255,255,0.06)` |
| 选中文字 / 强调 | `#BF5B29` | `#F49C6B` |

辅助文字在预览基础上适度加深，保证小字号可读性。普通文字与实际背景对比度至少 4.5:1；必要时微调相应语义色。悬停使用轻背景，键盘焦点使用清晰轮廓，选中态同时通过背景与字重表达。

## 4. 顶栏结构

左侧保留展开/收起控制、现有品牌或面包屑显示规则。右侧保留搜索、通知、主题、语言、工作台切换、用户菜单等当前可用入口；若真实代码按权限或配置显示额外入口，也必须保留。

搜索框使用低对比背景与细边框，桌面宽约 175px。快捷键保留现有 Ctrl/Cmd+K 行为，提示按平台显示。工作台入口采用透明背景、图标、名称和下拉箭头；用户入口采用圆头像、单行姓名与箭头。顶栏不再显示第二行角色标签，角色信息保留在现有用户菜单等详情位置。

通知、语言、用户等弹出层沿用现有组件与业务逻辑，颜色应跟随已解析主题，不另建一套弹出层系统。长姓名和工作台名允许省略，悬停或可访问名称可读取完整内容。

## 5. 侧栏与响应式

保留后端返回的菜单名称、顺序、分组、子菜单和权限过滤。子菜单沿用展开行为和路由选中逻辑，缩进表达层级，文字与图标统一规格。已有底部信息区域按同一排版和配色适配，不新增管理入口或重排菜单。

桌面收起行为保持完全隐藏（宽度 0），不新增图标轨道。展开后宽度为 224px，主布局左侧偏移必须同步。既有移动端抽屉继续复用侧栏内容，并保留遮罩、关闭与路由跳转行为。

响应式复用项目现有断点：1200px 以下收紧工具间距并缩短搜索框；992px 以下沿用现有移动/侧栏断点行为，将搜索呈现为图标入口，收短工作台名和用户名展示。更窄屏将非主要工具收纳到可访问的更多菜单，原有操作仍可到达；保留侧栏开关、搜索、工作台切换和用户入口。在 390px、768px、1024px、1440px 下不得出现导航横向溢出或控件互相覆盖。

## 6. 主题状态与组件边界

复用 `src/lib/design-system/theme.tsx` 中的 ThemeProvider 和 `itsm-theme` 存储键，以及 `ThemeHtmlClassSync` 的主题同步。不创建独立导航主题 store 或第二个存储键。

没有已保存偏好时默认 `light`。有效的 `light`、`dark`、`system` 历史偏好继续有效；`system` 根据操作系统解析成 A 或 C。顶栏主题按钮根据当前解析结果调用既有 `setMode`：浅色切深色，深色切浅色，不再从该按钮循环进入 system。其他已有设置入口的 system 支持保留。

读取偏好完成后才能写入，避免初次挂载默认值覆盖已存值。存储不可用时仍能在本次会话切换；非法存储值回到默认浅色，不导致渲染错误。刷新保持用户选择，初始恢复不出现可见的错误主题闪烁，SSR 与客户端不产生 hydration 报错。

组件职责：

- `header/Header.tsx` 及子组件负责顶栏呈现、现有动作入口和主题按钮交互。
- `sidebar/Sidebar.tsx`、`MenuItems.tsx` 负责真实菜单呈现，保留加载和失败状态。
- 共用导航语义变量负责字体、颜色、控件尺寸；在现有设计系统中定义一次，由顶栏、侧栏及必要的弹出菜单消费。
- `layout.config.ts` 及实际布局调用方负责一致的高度、侧栏宽度和内容偏移；清除受影响路径上冲突的 56/64px、256px 硬编码。

目前布局配置中把 64px 作为特定设计公式并进行断言。实施时应把本次明确的 60px 产品规格更新到相应说明与验证，不保留错误告警；不因此扩大为全站布局重构。

`components/layout/Header.tsx` 与 `Sidebar.tsx` 当前是重导出入口，真实实现位于子目录。修改真实实现，不复制一份新导航实现。移除受影响组件中被语义变量替代的冲突样式，不做无关文件迁移。

## 7. 数据流与错误处理

用户点击主题按钮 → 现有主题上下文更新 → 已有全局主题同步与导航变量切换 → 本地偏好保存。此流程不请求后端。

菜单加载、失败、空状态继续遵守现有 fail-closed 行为，不以样例菜单兜底。搜索、通知、登出和工作台切换的错误反馈继续使用原有路径。样式修改必须覆盖加载、禁用、选中、展开、焦点和弹出层状态，不以隐藏错误来改善外观。

## 8. 验收与交付

1. 同一真实页面分别验证 A/C：字体、尺寸、基线、边界与选中态符合本设计，切换无布局位移；预览仅作导航视觉参照。
2. 验证首次访问默认浅色、已有深色刷新恢复、历史 system、连续切换、非法存储值及存储不可用。为这些状态行为补充有意义的测试。
3. 验证桌面展开/收起、多级菜单、移动抽屉、长姓名/工作台名和四个目标宽度，无裁切、遮挡或横向溢出。
4. 浏览器验证搜索快捷键、通知、语言、工作台切换和用户菜单可用；验证键盘焦点与菜单可达性。
5. 运行受影响的现有测试、类型检查、静态检查及适用构建。新增测试聚焦主题状态和功能回归，不逐项断言 CSS 常量。
6. 保存本地视觉验证截图，交付时说明真实验证环境和未覆盖项；截图、预览服务器文件不进入提交。

## 9. 后续阶段：业务内容区样式统一

状态：backlog，已确认后续范围，尚未展开详细设计或实施。

在第一阶段顶栏与侧栏完成后，继续统一以下区域，使其与 A/C 双主题和已选排版风格协调：

- 页面背景：页面底色、内容容器与区域层次。
- 业务表格：表头、行高、文字、边界、悬停和选中状态。
- 卡片：背景、边框、圆角、间距和标题层级。
- 业务按钮：主次层级、尺寸、配色，以及悬停、焦点、禁用和加载状态。

后续阶段复用既有主题状态与设计系统，覆盖浅色和深色表现。启动时以真实业务页面梳理组件覆盖范围，单独完成设计、实施计划和验收；具体视觉规格届时确定，不从导航字号直接推导全部业务内容字号。本阶段的交付不代表上述 backlog 已完成。

本文仅为设计。当前未修改生产代码、运行应用验收、推送、创建 PR 或部署。用户审阅本文后进入 implementation plan 阶段，再按计划实现。
