# UI 工作台补齐：背景与第一阶段回归修复设计

- 日期：2026-09-14
- 状态：draft（第一阶段范围已获用户确认；本文待书面设计评审）
- 负责人：当前 Codex 任务；验收由维护者确认
- 基线：`origin/main` / `a25e108d2a08a55469fa5ad547aac5a9adc251ff`
- 分支：`codex/fix/ui-workbench-completion`
- Worktree：`/home/administrator/project/itsm/.worktrees/ui-workbench-completion`
- 当前交付：设计文档；尚未实施第一阶段修复

## 1. 决策背景与权威来源

本轮工作由 UI 重构实现审查触发。用户同意分阶段补齐，并要求将审查分析、方案取舍和依赖作为设计背景保存。第一阶段的单一目标是恢复工单详情工作台在视觉重构中丢失的操作能力，同时修复直接影响这些操作的遗留缺陷和错误状态。

本设计遵循 [AGENTS.md](../../../AGENTS.md)、[Agent 工程协作规范](../../agent-engineering-governance.md) 和 [开发与运维指南](../../DEVELOPMENT_GUIDE.md)。仓库根目录实际文件为 AGENTS.md，没有 AGENT.md。本设计应用现有架构约束，不修改其内容，因此本次无需修改 CLAUDE.md 的架构摘要。

相关历史设计：

- [角色分层 UI 与 AI 集成](2026-08-20-multi-persona-ui-and-ai-integration-design.md)：定义员工、工程师、主管、管理层与管理员入口。
- [工单详情工作台重构](2026-08-24-ticket-detail-workbench-redesign.md)：定义 8+4 双栏布局，明确要求原有功能、弹窗、权限门控和协作 Tabs 零丢失。
- [工单详情重构实施计划](../plans/2026-08-25-ticket-detail-workbench-redesign.md)：要求保留业务流，并进行类型检查、单测和真实路径验收。
- [设计 Token 统一](2026-08-27-frontend-design-token-unification-design.md)：定义独立的样式数据源收敛目标。

本文是现有工作台设计的修复补充，不替代角色产品方向，也不重新定义 WorkItem 或专业生命周期。

## 2. 已确认的审查发现

以下结论针对上述主线基线；文件位置从仓库根目录计算。后续分支有改动时须重新核对，不能把本表当作其他分支的当前状态。

| 编号 | 级别与归类 | 证据与用户影响 | 处理阶段 |
| --- | --- | --- | --- |
| R1 | P1，角色工作台未完成 | `src/app/(main)/workspace/tickets/page.tsx` 使用 MOCK_TICKETS，回复、转派、结单只显示成功提示；主管页的自动派单和重大事件合并同样未调用 API。入口是角色默认工作台，用户可能把提示当作真实执行结果。路径均位于 itsm-frontend 下。 | 第二阶段；第一阶段交付不代表角色工作台可生产验收 |
| R2 | P1，附件功能回归 | `7fa95a0a` 将 AttachmentPanel 换为 TicketAttachmentGrid，后者仅提供列表、缩略图和下载，丢失上传、删除和完整预览。空列表也没有上传入口。 | 第一阶段 |
| R3 | P2，关联功能回归 | `abfac65e` 将 RelationPanel 换为 TicketRelationCards，详情页丢失创建和删除关系的入口。 | 第一阶段 |
| R4 | P2，错误状态回归 | TicketCommentStream、TicketAttachmentGrid、TicketRelationCards 捕获读取失败后清空列表。403、500 和网络失败被显示为暂无数据，缺少重试入口。 | 第一阶段 |
| R5 | P2，重构前已有缺陷 | TicketDetail 分配 Select 的 label 是 React 元素，filterOption 却强制转为 string 后调用 toLowerCase。输入搜索触发 TypeError；类型断言不转换运行时值。 | 第一阶段，作为分配操作的直接相关修复 |

补充观察：ExecutiveDashboard 的 ChatBI 返回固定文字，置信度也是固定值，尚不是基于真实数据的分析。它属于第二阶段能力核查范围。

审查没有证明动态自定义字段编辑是本次重构引入的回归，不将其列为已确认缺陷。当前字段展示和编辑表单既有行为必须保留；新增字段编辑能力需另行定义范围。

### 审查验证证据与限制

- 当前基线执行 `npm run type-check -- --incremental false`，退出码为 0。
- 定向运行 TicketDetail 与 persona-config 两组 Jest 测试，18/18 用例通过；命令因全局覆盖率阈值未满足以 1 退出，不能据此宣称测试门禁通过。
- 用真实 React 元素调用相同过滤表达式，复现 `TypeError: option.label?.toLowerCase is not a function`。
- 已对照相关重构提交确认附件与关联替换；审查未执行浏览器实操、全量测试或生产构建。
- 测试生成的已跟踪 junit.xml 已恢复。本轮审查没有修改业务代码。
- 上述是审查基线证据，不替代新 worktree 的实施前基线和交付验证。

### 文档状态漂移

角色设计仍标待实施，工单工作台计划仍未勾选，而主线已有实现。Token 文档仍标待评审；不能据此认定所有 Token 目标已经实施或属于本轮回归。后续状态更新须引用实际提交和验收证据，不提前将完整 UI 重构标为 implemented。

## 3. 方案取舍与阶段边界

| 方案 | 收益 | 代价与结论 |
| --- | --- | --- |
| 分阶段恢复功能、接入真实数据、完成视觉验收 | 每个交付有明确边界，尽早恢复用户操作，便于独立回归和审查 | 推荐并已获用户同意；整体工作通过多个独立 PR 推进 |
| 一次补齐所有角色工作台 | 可统一交付所有角色体验 | 涉及队列、派单、统计和 AI 契约，范围过大，回归与审查风险高 |
| 先统一视觉与 Token | 改善样式一致性 | 无法解决操作丢失与虚假成功，且已有独立工作分支，不作为本轮起点 |

### 第一阶段：本分支首个交付

覆盖 R2–R5：附件管理、关联管理、分配搜索以及评论/附件/关联的读取错误、重试和操作后刷新。保留现有双栏布局、动作门控、服务申请面板、审批历史、通知和动态字段展示。

### 第二阶段：后续独立设计和 PR

工程师工作台接入真实队列、评论、分配和专业生命周期操作；主管与管理层页面分别核对统计、派单、报表和 AI 的可用契约。成功提示必须以实际操作结果为依据。未接入的功能应明确显示不可用，不能继续模拟成功。

这些能力尚未在本轮完成 API 盘点，不承诺全部可仅通过前端接线完成。后端能力缺口必须由所属领域解决；不可引入前端审批引擎、状态机或并行数据模型。

### 第三阶段：独立视觉收敛与文档验收

先核对设计 Token 和样式分支的实际成果，再确定集成方式。完成角色、桌面/移动端、主题与错误场景验收，更新对应文档状态。本分支不夹带全仓样式迁移。

## 4. 第一阶段设计

### 4.1 组件与数据所有权

- TicketDetail 负责路由身份、页面组合及父层计数同步，不承载附件和关系 CRUD 的第二套实现。
- 附件以现有 `components/business/detail-tabs/AttachmentPanel.tsx` 和 ticketAttachmentAdapter 为功能实现入口；在该组件内承接需要的工作台呈现。替换 TicketAttachmentGrid 的消费并检查其他消费者，无消费者后删除旧只读实现。
- 关联以现有 `components/ticket-relations/RelationPanel.tsx` 和 useTicketRelations 的查询/变更能力为入口，恢复工作台卡片呈现与创建、删除能力。替换 TicketRelationCards 的消费，无消费者后删除旧实现。
- 不机械挂回整个旧界面。RelationPanel 中的占位父子/依赖视图不能被作为已完成功能重新暴露；本阶段展示真实关系列表与已有关系类型，维持单一操作路径。
- TicketCommentStream 保留评论呈现与编辑器，继续消费 ticketCommentAdapter，补足错误、重试及请求身份处理。
- API 访问保留在 `src/lib/api/`，后端仍是权限、租户范围和关系合法性的权威来源。

### 4.2 附件与关联操作

- 附件空状态保留有权限用户的上传入口；上传展示进度，失败保留可重试状态；删除需确认；预览和下载走现有受保护接口。
- 恢复组件不等于证明底层 API 可用。实施时沿页面、adapter、API client、后端路由核对上传凭证、CSRF、文件限制和响应字段。若既有客户端契约阻止恢复操作，在原入口修复并补针对性测试。
- 关联支持选择目标工单、合法关系类型和说明；创建后刷新关系列表及计数；删除需确认，失败不得从列表永久移除。
- UI 门控遵循现有授权契约，不能从角色名称、persona、上传者身份或当前页面可见性自行推导授权。后端拒绝必须明确反馈。现有契约不足时记录并修复所属边界，不默认授权。
- 禁止自动扩大读取范围或跨租户关联；关系恢复不执行 Incident、Problem、Change 之间的生命周期转换。

### 4.3 错误、刷新与并发

- 首次读取失败：显示错误和重试，不能显示正常空状态；请求成功且结果为空时才显示暂无数据。
- 同一工单刷新失败：保留已有列表，明确标注刷新失败；服务端确认变更成功但随后刷新失败时，分别提示保存结果与刷新结果，避免诱导重复提交。
- 切换 ticketId：清除上一工单的数据、错误及未提交编辑状态；旧请求完成后不得覆盖新工单，也不得将上一工单草稿提交到下一工单。
- 写入进行中禁用重复提交；关闭弹窗不把未完成请求当作成功。
- 评论、附件、关联变更成功后，列表和父层 Tab 计数同步刷新。计数从对应查询结果派生，不增加第二份业务统计逻辑。
- 重试范围限制在失败面板；面板错误不能阻塞其他详情内容。

### 4.4 分配搜索

保留现有富文本选项呈现，为每个选项提供由姓名、用户名等现有展示字段组成的纯文本搜索值。过滤只对该字符串执行大小写归一化，移除用类型断言伪装字符串的做法。人员加载失败应可见且可重试，不能把权限拒绝显示为无人可分配；不扩展可分配人员的授权范围。

## 5. 影响面、依赖与隔离

预计涉及 TicketDetail、AttachmentPanel、RelationPanel、TicketCommentStream、关系 hooks、相应 API client（仅在恢复功能所需的契约修复范围内）及相邻测试。删除仅用于被替代且无消费者的 TicketAttachmentGrid、TicketRelationCards；不删除其他专业详情页依赖的共享组件。

已知分支事实，核查日期为 2026-09-14：

- `fix/ticket-detail-console-errors-and-contracts` 相对当前 main 仅多出一次测试结果提交，无额外生产修复可直接集成。
- 设计 Token、frontend-style-system、visual-theme-unification 已有独立 worktree；本阶段不直接合并或改动它们。
- `codex/refactor/workitem-convergence-relations` 存在关系与 WorkItem 后续工作。实施前比较本阶段需要的 API/类型与该分支改动，记录契约冲突；不因本地存在分支就假定已合并或已部署。
- 原主工作区有未跟踪 `docs/poc/`，不属于本任务，不复制或改动。

第一阶段不计划 schema 迁移、共享数据库清理、初始化或批量修复。浏览器真实路径验证中的上传、评论、关系创建/删除属于测试数据写入，应使用隔离测试租户/环境，并记录目标与清理边界，不操作业务数据。若发现必须变更共享基础设施，按工程治理规范重新登记影响范围。

## 6. 验收标准

| 范围 | 必须证明的结果 |
| --- | --- |
| 附件 | 空列表可上传；受允许文件上传成功后重新读取可见；删除取消不发请求、确认后重新读取不可见；预览/下载可用；超限和失败反馈明确 |
| 关联 | 创建/删除正确传递当前工单和目标身份；重新读取结果正确；拒绝请求不提示成功；关系类型与方向保留；无占位成功页面 |
| 搜索 | 输入姓名/用户名不会抛出 TypeError；正确匹配；空输入恢复列表；人员读取失败可重试 |
| 读取状态 | 评论/附件/关联分别覆盖首次失败、重试成功、正常空列表、刷新失败保留旧数据，以及切换工单时旧请求晚到 |
| 刷新与计数 | 写入成功后列表和 Tab 计数一致；刷新失败明确提示；重复点击不重复提交 |
| 边界 | 无权限和跨租户拒绝按后端契约反馈；页面可见或切换 persona 不赋予额外操作权 |
| 既有功能 | 原动作门控、编辑/抄送弹窗、动态字段展示、服务申请、审批历史和通知行为不回归 |
| 视觉交互 | 桌面与 390px 宽度下，附件、关联、搜索、弹窗及错误提示可用，无新增横向溢出；核查 light/dark 的受影响区域 |

实施前运行受影响的最小基线；使用相邻 Jest 测试验证行为与错误分支，不只断言组件挂载。窄范围测试可显式关闭覆盖率收集，结果不能替代仓库全局覆盖率门禁。交付运行与影响范围匹配的 type-check、lint:check、相关测试和生产构建；涉及 API 契约时增加契约检查。真实路径按仓库 E2E 指南执行，并记录浏览器实际服务的源码版本，不能将旧镜像截图作为新分支验收。

实施计划必须把上述验收项映射到具体测试或人工验证步骤。不能自动化或因环境未验证的项目需明确记录，不以通过类型检查替代功能完成。

## 7. 文档交付与后续流程

本文经维护者书面评审后，将状态改为 accepted，再编写第一阶段实施计划。第一阶段验收通过后记录提交与验证证据，并同步历史工单工作台计划的对应条目；不能顺带宣称第二、三阶段完成。

首个 PR 只交付工单详情回归修复及其直接依赖。后续角色工作台和 Token 工作继续以独立目标、分支、PR 和验收记录推进。提交与目录规则引用工程治理规范，不在本文重复定义。
