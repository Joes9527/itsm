Transition-prefix verifier assets are added here when a release publishes a
schema-changing upgrade from a retained source release. The 028 release has no
incoming transition because it is the current minimum supported baseline.
